{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "Graven",
  "icon": "assets/graven/icon.png",
  "description": "",
  "authors": [
    "Chen_Meng",
    "06789"
  ],
  "entrypoints": {
    "client": [
      "tech.hakuri.graven.fabric.GravenFabric"
    ],
    "graven:addon": [
      "tech.hakuri.graven.fabric.addon.FabricSelfAddonEntrypoint"
    ]
  },
  "license": "GPL-3.0-only",
  "environment": "client",
  "mixins": [
      "${mod_id}.mixins.json",
      "${mod_id}.fabric.mixins.json"
  ],
  "accessWidener": "${mod_id}.accesswidener",
  "depends": {
      "fabricloader": ">=${fabric_loader_version}",
      "fabric-api": "*",
      "minecraft": "~${minecraft_version}",
      "java": ">=${java_version}"
  }
}
